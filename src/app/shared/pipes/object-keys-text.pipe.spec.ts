import { ObjectKeysTextPipe } from './object-keys-text.pipe';

describe('ObjectKeysTextPipe', () => {
  it('create an instance', () => {
    const pipe = new ObjectKeysTextPipe();
    expect(pipe).toBeTruthy();
  });
});
